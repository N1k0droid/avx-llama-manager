# avx1-llama-manager
A PowerShell toolkit to build and run llama.cpp with CUDA on older Windows workstations. Bypasses AVX2/FMA crashes on legacy CPUs (AVX1-only) via guided MSVC builds, featuring tray management and VRAM safeguards.
